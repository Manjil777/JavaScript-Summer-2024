/* 
how to write path:


 {  . } means its own folder
   {  .. } means parent folder
     {  ~ } means home folder
       { / } is used to say aroko
       { .../ } is not possible
*/

/* 
export by name:
 in a module or file we can export oen or more variables
 {} braces is used to import
  while import we must use same name that is used for export.


export by defalut ( no need curly braces and same name)

in a module  or file we can export only one variable
{} braces is not used to import
 while import it is not necessary to use same name i.e used to export that  is we use different name.
 
*/
