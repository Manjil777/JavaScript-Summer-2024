console.log(`sum of 1+1 is ${1 + 1}`);
console.log(`Difference of 1+1 is ${1 - 1}`);
console.log(`Multipication of 1+3 is ${1 * 3}`);
console.log(`Division of 4/2 is ${4 / 2}`);

let name = "manjil";
let surname = "budhathoki";

console.log(`hello ${(name, surname)} sir`);

/* 
We can perform js operation inside ${}
eg +,-,*,/,.... variable call , function call.

${} return only one value 
*/
